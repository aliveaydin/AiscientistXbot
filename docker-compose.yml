services:
  bot:
    build: .
    container_name: aiscientist-bot
    restart: always
    expose:
      - "8000"
    ports:
      - "8080:8000"
    volumes:
      - bot-data:/app/data
    env_file:
      - .env.production
    environment:
      - DATA_DIR=/app/data
      - ARTICLES_DIR=/app/data/articles
      - DATABASE_URL=sqlite+aiosqlite:////app/data/bot.db
      - FRONTEND_DIST_DIR=./frontend_dist

  website:
    build:
      context: .
      dockerfile: Dockerfile.website
      args:
        NEXT_PUBLIC_API_URL: http://bot:8000
    container_name: kualia-website
    restart: always
    expose:
      - "3000"
    depends_on:
      - bot

  nginx:
    image: nginx:alpine
    container_name: kualia-nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - bot
      - website

volumes:
  bot-data:
